generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = "postgres://postgres:otavio@localhost:5433/valida-cpf"
}

model Validation {
  id          Int @default(autoincrement())@id
  valid       Int
  invalid     Int

  @@map("validations")
}