generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = "postgresql://postgres:otavio@18.228.119.12:5432/validacpf?schema=public"
}

model Validation {
  id          Int @default(autoincrement())@id
  valid       Int
  invalid     Int

  @@map("validations")
}